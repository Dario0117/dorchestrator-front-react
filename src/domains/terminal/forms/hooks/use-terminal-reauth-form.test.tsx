// useTerminalReauthForm is tested through terminal-reauth-modal.test.tsx
// The hook wires TanStack Form with the terminal reauth schema and mutation,
// containing no standalone logic beyond form configuration.

test('useTerminalReauthForm — tested via terminal-reauth-modal.test.tsx', () => {
  expect(true).toBe(true);
});
